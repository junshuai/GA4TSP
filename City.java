public class City {
    /** The 2D coordinate attributes is public so that it can be called easily. */
    public double x, y;

    public City(double x, double y) {
        this.x = x;
        this.y = y;
    }
}
